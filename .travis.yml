language: generic

sudo: required

services:
    - docker

before_install:
    - docker build -t jsjung/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
    - docker run -e CI=true jsjung/react-test-app npm run test

after_success:
    - docker build -t jsjung/docker-frontend ./frontend
    - docker build -t jsjung/docker-backend ./backend
    - docker build -t jsjung/docker-nginx ./nginx

    - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

    - docker push jsjung/docker-frontend
    - docker push jsjung/docker-backend
    - docker push jsjung/docker-nginx
